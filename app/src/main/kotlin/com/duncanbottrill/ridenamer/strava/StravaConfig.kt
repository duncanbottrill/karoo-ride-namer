package com.duncanbottrill.ridenamer.strava

/**
 * Base URL of your deployed Strava token-exchange backend (see `/backend`). This is NOT
 * secret — it's safe to commit. Replace with your Vercel production URL, no trailing slash.
 */
const val STRAVA_BACKEND_URL = "https://backend-opal-psi-74.vercel.app"
